# 5 zadań praktycznych z konsoli JavaScript

Oto zestaw zadań, które pozwolą Ci przećwiczyć korzystanie z różnych metod obiektu `console` w JavaScript.

---

### Zadanie 1 (Podstawy)
Stwórz dwie zmienne: `imie` oraz `miasto`. Wypisz je w konsoli za pomocą zwykłego `console.log()`, łącząc je przecinkami, aby uzyskać zdanie: `[Imię] mieszka w [Miasto].`

```javascript
// Twoje rozwiązanie tutaj:
```

---

### Zadanie 2 (Interpolacja)
Zrób dokładnie to samo co w Zadaniu 1, ale tym razem użyj nowoczesnych szablonów tekstowych (template literals z użyciem znaków `` ` `` oraz `${}`).

```javascript
// Twoje rozwiązanie tutaj:
```

---

### Zadanie 3 (Struktury danych)
Utwórz tablicę z trzema ulubionymi owocami oraz obiekt opisujący Twój ulubiony samochód (klucze: `marka`, `model`, `rok`). Wypisz oba te elementy w konsoli za pomocą `console.log()`.

```javascript
// Twoje rozwiązanie tutaj:
```

---

### Zadanie 4 (Tabelaryzacja danych)
Stwórz tablicę składającą się z przynajmniej trzech obiektów. Każdy obiekt niech reprezentuje znajomego (klucze: `imie`, `wiek`, `miasto`). Użyj metody `console.table()`, aby wyświetlić te dane w formie czytelnej tabeli.

```javascript
// Twoje rozwiązanie tutaj:
```

---

### Zadanie 5 (Warunkowe błędy)
Stwórz zmienną `liczbaPrób` o wartości `4`. Napisz prostą instrukcję warunkową (`if / else`): jeśli liczba prób jest większa niż 3, wypisz w konsoli błąd za pomocą `console.error()`. W przeciwnym razie wypisz zwykły komunikat sukcesu.

```javascript
// Twoje rozwiązanie tutaj: